// Re-export from feature location for backward compatibility
export { requireFirebaseAuth, requireAdmin, requireDebugAccess, preventGetMethod } from '../features/auth/middleware/auth'
export type { AuthContext } from '../features/auth/middleware/auth'
