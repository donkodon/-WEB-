// Re-export from shared location for backward compatibility
export { createSafeErrorResponse, sanitizeErrorMessage, logError, ErrorCode } from '../shared/helpers/error-handler'
export type { SafeErrorResponse } from '../shared/helpers/error-handler'
